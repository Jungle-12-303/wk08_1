
### 공부할 내용
1. FD table에서 file table entry 주소를 어떻게 찾아가는거지? + file table, v-node에 대한 정보는 어디서 가지고 오는거지?
ㄴ 디렉토리에 i-node 정보가 들어가 있음 그 i-node로 파일을 열면 메모리에 파알 테이블 엔트리 생성 그 주소는 FD 테이블에 매핑
ㄴ 디렉토리 주소는 어떻게 알고 찾아가는거지? -> 루트 부터해서 탐색 루트의 i-node 번호는 고정
 
3. V-node는 왜 필요한거지?
   i-node만으로 안되는 이유는 파일 시스템 종류가 다양하기 때문 -> 파일 시스템 형식이 다르면 추상화 해줄 무언가가 필요 그게 v-node
   
5. Socket, Sock 구조체 내부에는 어떤 값이 들어가는거지?
ㄴ 서버 구현하면서 찾아볼 예정


7. rio 함수들은 일반적인 file 함수와 어떤 차이점을 가지고 있는거지?
ㄴ 버퍼가 요구하는 데이터 크기보다 작을 경우 데이터를 다 받아낼 수 있도록 해주는 함수임. -> while문으로 작동


### 질문
1. 앱을 터미널에서 켰을 경우와 GUI 아이콘 클릭으로 켰을 경우 FD table에서 0, 1, 2번 값이 다른 이유? -> 터미널인 경우 0, 1, 2모두 /dev/ttys000, GUI인 경우 /dev/null

